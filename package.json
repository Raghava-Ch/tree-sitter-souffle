{
  "name": "tree-sitter-souffle",
  "version": "0.1.0",
  "description": "",
  "main": "bindings/node",
  "types": "bindings/node",
  "scripts": {
    "test": "tree-sitter test",
    "install": "node-gyp-build",
    "prebuildify": "prebuildify --napi --strip"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/langston-barrett/tree-sitter-souffle.git"
  },
  "author": "Langston Barrett",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/langston-barrett/tree-sitter-souffle/issues"
  },
  "homepage": "https://github.com/langston-barrett/tree-sitter-souffle#readme",
  "dependencies": {
    "node-addon-api": "^7.1.0",
    "node-gyp-build": "^4.8.0"
  },
  "peerDependencies": {
    "tree-sitter": "^0.21.1"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.20.7",
    "prebuildify": "^6.0.0"
  },
  "tree-sitter": [
    {
      "scope": "source.dl",
      "file-types": [
        "dl"
      ],
      "injection-regex": "^(dl)$"
    }
  ],
  "files": [
    "grammar.js",
    "binding.gyp",
    "prebuilds/**",
    "bindings/node/*",
    "queries/*",
    "src/**"
  ]
}
